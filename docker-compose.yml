services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pm_agent
    ports:
      - "5433:5432"
    volumes:
      - pm_agent_pg_data:/var/lib/postgresql/data

volumes:
  pm_agent_pg_data:
